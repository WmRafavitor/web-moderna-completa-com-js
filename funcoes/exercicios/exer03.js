// Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base
// elevada ao expoente.

function potentiation(base, expoent) {
    let result = 0;
    result = base ** expoent;
    console.log(`O resultado é ${result}`);
}

potentiation(5, 5);